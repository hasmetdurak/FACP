version: '3.8'
services:
  facp:
    build: .
    ports:
      - "8001:8001"
    environment:
      - SECRET_KEY=your-secret-key
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
